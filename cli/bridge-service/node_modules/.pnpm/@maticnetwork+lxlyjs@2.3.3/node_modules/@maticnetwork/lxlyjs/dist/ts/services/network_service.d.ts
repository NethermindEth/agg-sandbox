import { HttpRequest } from "../utils";
export declare class NetworkService {
    httpRequest: HttpRequest;
    constructor(baseUrl: string);
    private createUrl;
    getMerkleProof(networkID: number, depositCount: number): Promise<any>;
    getBridgeTransactionDetails(networkID: number, depositCount: number): Promise<any>;
}
