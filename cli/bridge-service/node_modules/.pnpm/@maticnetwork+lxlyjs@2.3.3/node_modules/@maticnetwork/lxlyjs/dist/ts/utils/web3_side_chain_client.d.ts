import { IBaseClientConfig, IProviders } from "../interfaces";
import { ABIService } from "../services";
import { Logger } from "./logger";
export declare class Web3SideChainClient<T_CONFIG> {
    providers: IProviders;
    config: T_CONFIG;
    abiManager: ABIService;
    network: string;
    logger: Logger;
    resolution: {};
    init(config: IBaseClientConfig): Promise<string>;
    getDefaultConfig(networkId: number): {
        from?: string;
    };
    getConfiguration(networkId: number): {
        bridgeAddress?: string;
        wrapperAddress?: string;
        gasPorterAddress?: string;
        isEIP1559Supported?: boolean;
    };
}
